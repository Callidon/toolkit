{
	"name": "react-todolist",
	"version": "1.0.0",
	"description": "Experimentations with React",
	"main": "index.html",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "browserify -t [ babelify --presets [ react ] ] js/main.js -o js/bundle.js",
		"serve": "live-server"
	},
	"author": "Thomas Minier",
	"license": "MIT",
	"dependencies": {
		"babel-preset-react": "^6.3.13",
		"babelify": "^7.2.0",
		"react": "^0.14.5",
		"react-dom": "^0.14.5"
	},
	"xo": {
		"globals": [
			"document",
			"marked"
		]
	}
}
